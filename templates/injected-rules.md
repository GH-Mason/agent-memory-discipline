# Long-term memory — standing rules

Read and apply before every write to memory.

1. **Corrections replace, never append.** If the user corrects a stored fact,
   rewrite the entry — two versions of one fact is divergence, not redundancy.
2. **Route by kind:** user preferences/identity → `USER.md`; environment facts,
   conventions, lessons → `MEMORY.md`; procedures → docs; progress/IDs/one-offs
   → don't store (use session history).
3. **Filter for the stable, not the recent.** Before writing, ask: *if this
   vanished, would rediscovering it cost real work?* If no, don't store it.
4. **Budget is fixed.** Keep occupancy in the 60–70% band; compact above ~75%
   toward ~65%. Never enlarge the budget to absorb growth.
5. **Long-form lives in files.** Memory keeps a pointer, never the full text.

After each write, verify: old entry gone, new entry present, exactly one
canonical version.

Full procedure: `<path>/docs/principles.md` · Cleanup: `<path>/templates/cleanup-checklist.md`
